"""Canonical manifest for the external verification sweep.

The suite list is the single source of truth for what OpenCode runs. Any new
verifiable suite must be registered here to be part of the readiness decision.
"""
from __future__ import annotations

SUITES = (
    "test_math_expert.py",
    "test_math_coordinator.py",
    "test_math_integrity.py",
    "test_world_stability_drift.py",
    "test_semantic_channels.py",
    "test_visual_embodiment.py",
    "test_canonical_validation.py",
    "test_platform_layers.py",
    "test_portable_runtime.py",
    "test_readiness.py",
    "test_verification_sweep.py",
    "test_rig_math.py",
    "test_learning_prediction.py",
    "test_safety_anomalies.py",
    "test_agent_collaboration.py",
    "test_task_orchestration.py",
    "test_current_pc_safety.py",
    "test_wireframe_face.py",
    "test_accuracy_comparison.py",
    "test_intelligence_loop.py",
    "test_intelligence_bridge.py",
    "test_verification_hardening.py",
    "test_verdict_authentication.py",
    "test_independent_verification.py",
    "test_jcs_canonicalization.py",
    "test_canonical_representation.py",
    "test_state_bus.py",
    "test_representation_detector.py",
    "test_world_mapping.py",
    "test_persona_fusion.py",
    "test_face_state.py",
    "test_face_runtime.py",
    "test_temporary_embodiment.py",
    "test_semantic_visual_bridge.py",
    "test_ui_qt.py",
    "test_ui_thread_protection.py",
    "test_math_substrate.py",
    "test_conversational_intelligence.py",
    "test_canonical_vector_face.py",
    "test_cooperative_orchestration.py",
    "test_integrated_validation.py",
    "test_generalization.py",
    "test_persona_boundaries.py",
    "test_identity_rollback.py",
    "test_identity_behavior_integration.py",
    "test_identity_visual_integration.py",
    "test_runtime_integration.py",
    "test_system_coherence.py",
    "test_application_reality.py",
    "test_canonical_face_presentation.py",
    "test_router_parity.py",
    "test_report_staleness.py",
    "test_evaluation_harness.py",
    "test_session_recap.py",
    "test_conversation_face.py",
    "test_living_reports.py",
    "test_evidence_drilldown.py",
    "test_capability_awareness.py",
    "test_restoration_math_knowledge_orchestration.py",
    "test_restoration_runtime_device_bridge_loop.py",
    "test_trusted_capabilities.py",
    "test_trust_commands.py",
    "test_trust_live_validation.py",
    "test_intelligence_core.py",
    "test_ux_redesign.py",
    "test_frequency_architecture.py",
    "test_frequency_external_validation.py",
    "test_provisioning_recovery.py",
    "test_full_system_integration.py",
    "test_orchestration_lifecycle.py",
    "test_pending_validation.py",
    "test_instrumentation.py",
    "test_performance_contracts.py",
    "test_async_orchestration.py",
    "test_async_ui_contracts.py",
    "test_chat_process.py",
    "test_chat_process_ui.py",
    "test_cancellation_stale.py",
    "test_output_supervision.py",
    "test_bridge_updates.py",
    "test_trust_cache.py",
    "test_retention_mailbox.py",
    "test_observability.py",
    "test_market_pattern_module.py",
    "test_market_analyst_persona.py",
    "test_financial_safety_rules.py",
)

# Contracted evidence: the exact number of ``=OK`` markers each registered
# suite emits on a successful run (established by executing every suite).
# The runner requires an exact match before a suite may be clean, so silent
# or truncated suites can never certify the battery.
SUITE_CONTRACTS = {
    "test_math_expert.py": {"expected_ok": 11},
    "test_math_coordinator.py": {"expected_ok": 21},
    "test_math_integrity.py": {"expected_ok": 16},
    "test_world_stability_drift.py": {"expected_ok": 10},
    "test_semantic_channels.py": {"expected_ok": 10},
    "test_visual_embodiment.py": {"expected_ok": 10},
    "test_canonical_validation.py": {"expected_ok": 6},
    "test_platform_layers.py": {"expected_ok": 12},
    "test_portable_runtime.py": {"expected_ok": 12},
    "test_readiness.py": {"expected_ok": 10},
    "test_verification_sweep.py": {"expected_ok": 10},
    "test_rig_math.py": {"expected_ok": 15},
    "test_learning_prediction.py": {"expected_ok": 11},
    "test_safety_anomalies.py": {"expected_ok": 11},
    "test_agent_collaboration.py": {"expected_ok": 11},
    "test_task_orchestration.py": {"expected_ok": 11},
    "test_current_pc_safety.py": {"expected_ok": 5},
    "test_wireframe_face.py": {"expected_ok": 14},
    "test_accuracy_comparison.py": {"expected_ok": 17},
    "test_intelligence_loop.py": {"expected_ok": 19},
    "test_intelligence_bridge.py": {"expected_ok": 10},
    "test_verification_hardening.py": {"expected_ok": 25},
    "test_verdict_authentication.py": {"expected_ok": 11},
    "test_independent_verification.py": {"expected_ok": 10},
    "test_jcs_canonicalization.py": {"expected_ok": 9},
    "test_canonical_representation.py": {"expected_ok": 19},
    "test_state_bus.py": {"expected_ok": 37},
    "test_representation_detector.py": {"expected_ok": 61},
    "test_world_mapping.py": {"expected_ok": 71},
    "test_persona_fusion.py": {"expected_ok": 72},
    "test_face_state.py": {"expected_ok": 19},
    "test_face_runtime.py": {"expected_ok": 20},
    "test_temporary_embodiment.py": {"expected_ok": 15},
    "test_semantic_visual_bridge.py": {"expected_ok": 13},
  "test_ui_qt.py": {"expected_ok": 17, "note": "8E Qt Quick interface bridge dispatch and fallback plus Phase 4E cold-start skeleton + pipeline-visibility wiring"},
  "test_ui_thread_protection.py": {"expected_ok": 20, "note": "9A UI-thread protection: pure-bridge pulled projection parity/isolation/reset/frame/no-mutation, ordered projection worker ordering/bounds/fail-closed/shutdown, Tk serialized compute worker (projection off the UI thread, widget apply on the UI thread, fail-closed reset + recovery, bounded single-slot skip backpressure, close drains and joins the compute thread), Qt controller projection pipeline (ordered executor, UI-thread snapshot/advance/apply with the pull on the worker, delivered-view parity, fail-closed reset + recovery, bounded non-blocking backpressure, closeNow joins + rejects)"},
  "test_math_substrate.py": {"expected_ok": 44, "note": "8F mathematical substrate; oracle-agreeing analytic/statistics/probability/information/graph/logic plus evidence composition"},
  "test_canonical_vector_face.py": {"expected_ok": 35, "note": "8M canonical vector face; identity-derived features with provenance, landmark positions, iris/pupil/lens/nose/mouth math, symmetry pairs, authority mirror consistency, unit-space bounds, deterministic geometry digest, verified-anchor expression vocabulary (no invented emotion), bounded expressions at channel ceilings, VisualState/VisualCommand/FaceState flows, renderer separation boundedness and no-motion determinism, QML JSON payload, activation gate + version record"},
  "test_conversational_intelligence.py": {"expected_ok": 23, "note": "8G conversational intelligence; state/topics/references/ambiguity/corrections/epistemic labels, psychology-never-diagnosis, philosophy-DISPUTED, justified 8F math, text/voice parity, voice interfaces INTERFACE ONLY, fail-open, purity, determinism, epistemic-parity"},
  "test_cooperative_orchestration.py": {"expected_ok": 219, "note": "8H cooperative orchestrator+epistemics+adaptive latency+fault tolerance+resource awareness+cooperation math+learning ledger+method selection+capability profiles; verdicts, epistemic statuses, static depth, circuit breaker, resource boost, concordance/combine/label-similarity, outcome ledger, breaker-ledger closed loop, scored method registry, gated improvement, per-user default-deny capabilities, fail-open, purity, determinism; rp_greeting_pressure_standard + ms_plan_nonfinite_safe resolved (were FAIL), certified count 219 = fresh measurement"},
  "test_integrated_validation.py": {"expected_ok": 11, "note": "8H phase 11 integrated validation; capability gate -> conversation turn -> cooperation/epistemic -> adaptive depth -> guarded execution with ledger closed loop -> scored method selection -> cooperation math -> gated improvement -> whole-flow determinism -> cross-module purity -> export surface parity"},
  "test_generalization.py": {"expected_ok": 309, "note": "8I full pipeline: task representation, memory, similarity, hypotheses, unseen testing, transfer boundaries, error generalization, method portfolio, adaptive orchestration, learning metrics, controlled self-improvement, cooperative challenge, resource-aware profiles; purity, exports"},
  "test_persona_boundaries.py": {"expected_ok": 14, "note": "8J-A persona boundary battery: cognitive authority protocol/source, import surface, reference resolution, presentation-cannot-modify-cognition separation, bounded channels, identity/memory write isolation, no memory/identity deps, stale reference absent, no duplicate cognitive authority, legacy persona_layer defined state (deprecated), broken imports removed, core identity intact"},
  "test_identity_rollback.py": {"expected_ok": 6, "note": "8J-A identity rollback design support: protocol, prior-version targets only, plan shape (prepared/not executed/no autonomy/operator approval/protected paths), rejects current version, pure (no writes), deterministic"},
  "test_identity_behavior_integration.py": {"expected_ok": 19, "note": "8J-B identity-to-behavior pathway: contract module + architecture-backed principles, immutable/deterministic context, identity influence + provenance + version sensitivity, bounded constraints, verification priority (no gate bypass/false confidence), presentation isolation from identity and reasoning, identity after interpretation/before generation, deterministic bridge pathway, identity source registry intact, write-free behavior, conversation memory never composes identity"},
  "test_identity_visual_integration.py": {"expected_ok": 21, "note": "8J-C identity-to-embodiment pathway: identity-verified VisualCommand contract module + architecture-backed visual rules, immutable/deterministic command, identity consistency / visual determinism, version detectability, provenance, bounded params + honest expression vocabulary, renderer boundary protection (renderer cannot modify identity), renderer raster purity (identity change cannot change output), expression limits within channel ceilings, identity absent from renderer/semantic layers, write-free build, validated activation gate recorded via identity_versions (no silent drift), presentation isolation, maya_app tick wiring"},
  "test_runtime_integration.py": {"expected_ok": 87, "note": "8J runtime adaptive integration: disabled-by-default env gate, seam capture in production chat path + garbage-attr protection, forwarded state via run_context, capture shape/consistency/determinism/no mutating input/privacy strip + private roundtrip, raw text never in key, ledger correction records, portfolio outcomes + pledge acknowledgement, prefer/reject advisory + narrow-NOI gating + success outcome, seen-category generalization to unseen cases, math-precise generalization and promotion after two, promoted status, lean-fast bypass/dispatch, full_standard generalization to unseen, cooperative challenge vocabulary mini-task override + counting plus objectives, no-fire on clean, fired on leaking, portable rollback records, cross-field summary, no rollback without preimage, deterministic challenge output, proactive sticky no-critique"},
  "test_system_coherence.py": {"expected_ok": 99, "note": "8N system coherence + identity boundaries + architectural integrity: identity write confinement and on-disk/ledger hash stability across conversation/learning/profile flows, _NON_FACTUAL tagging and never-OBSERVED user claims, conversation turns never write world evidence and never learn private memory, failed hypotheses rejected and never become memory, world model stores no decisions + conflict flags + breaking stability, lowercase cooperation verdicts and honest disagreement dimensions, deterministic pure orchestrator, math substrate invariants (non-certifying), unknown confidence rejected, epistemic ladder completeness + no-fact status, adaptive promotion gates (evidence + repeated success) + rollback + failure boundaries, controlled improvement gates (old-status/similarity/evidence) + cooperative challenge weaken, capability default-deny grants never touch identity, psychology engaged-but-tentative on distress with no diagnosis, unapproved context rejected, vector-face expression payload divergence without geometry rewrite + deterministic stateless render, circuit breaker trip/hold/half-open/recovery, guarded fail-open no-raise, adaptive seam env-gated off + fail-open, static UI boundary scans, adaptive never writes files"},
  "test_application_reality.py": {"expected_ok": 24, "note": "8N application reality: session id uniqueness + exchange roundtrip, local read-only commands (:mission,:status detail,:world summary,:world compare,:intent, owner identity statement), opening intent classification, math task answer path, philosophy route compared, learning promotes after two, identity readiness + vector face + avatar path availability, profile default-deny + explicit grant, failed segment recoverable, unnormed input fail-open, offline fallback greeting + governance statement, adaptive seam offline by default"},
  "test_canonical_face_presentation.py": {"expected_ok": 22, "note": "8M canonical face presentation; bridge-emitted canonical vector-face payload with schema/digest recompute, known ops, determinism + no-motion defaults (frame is metadata), bounded verified-vocabulary expressions, independent authority-JSON geometry oracle (iris/mouth/nose), QML presentation-only (single face surface, no embedded identity geometry, no randomness/time reading, schema-gated payload), sabotage drills A-G (landmark mutation, symmetry violation, expression overflow, renderer identity write, VisualState bypass, QML duplication, random/clock dependency) all fail"},
  "test_router_parity.py": {"expected_ok": 11, "note": "single-owner router parity; deterministic NL answers (math, owner identity, maya identity, state, greeting, capability) identical across _conversation_route/maya_local_command/local_fallback/ask entry points, ask() resolves the owner before the model path, colon-command partition intact (no NL interception of :verbs), offline fallback governance lines + owner-extended offline greeting pinned, Andy-Warhol research separation survives parity, route determinism byte-identical, continue-later thread phrases (resume/continue against recap threads) resolve identically on every surface with passthrough/ambiguity preserved"},
  "test_report_staleness.py": {"expected_ok": 10, "note": "report staleness; file-backed review-only reports served with generated_at older than the freshness window carry a structured staleness block + warning inside the JSON (disk never written, payload never mutated), fresh/at-limit/unparseable reports unflagged, all five real review reports (:review/:income/:evidence/:continuity/:focus) flag stale, serving stays JSON-parseable, classification pure and deterministic under an injected clock, non-dict passthrough"},
  "test_evaluation_harness.py": {"expected_ok": 10, "note": "shared evaluation harness; canonical render_json (ensure_ascii=False + trailing newline) and render_markdown idiom, write_artifacts+read_json roundtrip, score_patterns hits/misses/coverage with empty-set guard, ratio zero-total guard, mean_round empty guard, Curie/NASA fixture lists pinned verbatim, all converted writer scripts import and expose entrypoints (run/main), converted writers delegate artifact output to the harness, harness modules contain no randomness or clock dependency"},
  "test_session_recap.py": {"expected_ok": 33, "note": "session recap; store load_events keeps full event rows, digest deterministic for same log bytes and under event reorder, empty log handled, digest structure complete, questions from user ?-turns only, open follow-ups detected by real Maya deferral vocabulary (human approval, not available yet, would you like, say the word), most-recent-session selection capped by availability, pause pure-mode assigns thread id without writes, resume flips status + counts, unknown thread errors, blank topic rejected, write-mode state confined to knowledge/conversations/recap_threads.json, resumed threads leave open list, marker text pins thread to containing session, full command surface (:recap [3|7], pause, resume, help, usage fallback), router :recap wiring via maya_local_command, non-recap input passthrough, research answers carry a registry-derived evidence-drilldown tip only when evidence IDs were actually registered (no tip, nothing fabricated on empty answers), real-log digest deterministic and byte-read-only, report renders, NL continue-phrase resolver, resume-thread-id phrasing incl. politeness/punctuation, bare-thread ambiguity, honest no-match, non-continue passthrough) pure via state injection, write-mode surface parity with fresh state per surface (resume is a write) persisting through the single thread-state writer, bare continue stays ambiguous everywhere"},
  "test_conversation_face.py": {"expected_ok": 11, "note": "conversation-driven face; wire protocol bounded to the four activity keys that map onto pre-validated semantic anchors in face_drive (listening->listening, processing/research->focused, idle->neutral; no new parameter surface, channel ceilings untouched), parser tolerant to prompt-merged/malformed/unknown lines (None keeps prior activity), typing overlay policy owned by face_drive.typing_activity (only a resting idle face lifts to listening; processing/research always win and are never degraded; overlay value is a pre-validated anchor; TYPING_SETTLE_SECONDS shared by both interfaces), Tk KeyRelease hook lifts the face while typing and schedules the settle, producer-driven states cancel the pending settle, settle dispatches through the single state owner with a working-state guard (never degrades research), Qt QML Keys.onPressed notifies the controller, timer stopped only on the GUI thread (chat reader thread never touches the QTimer — thread-safety invariant pinned), late settle fire is a guarded no-op, producer emits only allowlisted states and only under MAYA_FACE_LINES=1, both interface spawners enable the protocol and share face_drive.parse_face_line, real spawned chat turn sequence listening-first bounded-throughout idle-at-settle, research turn passes through the research phase and settles to idle, plain terminal use emits no face lines"},
  "test_living_reports.py": {"expected_ok": 11, "note": "living reports; pure structural delta (keys added/removed, scalar changes, list sizes; bookkeeping keys excluded; inputs never mutated; unchanged detected), deterministic generated_at archive stamps with idempotent re-archive and report file untouched, refresh without a registered generator archives honestly and fabricates nothing (report unchanged, banner stays truthful), unknown report errors without generator but works with an explicitly injected one, injected/registered generator regenerates with delta and rejects non-dict output preserving the prior report, registry maps the full five-report review surface with label+generator fields, :report command surface (status, refresh, delta, help, usage fallbacks, non-report passthrough), delta command compares live report against latest archive and reports no_archive honestly, serving and status remain byte-write-free with staleness banners intact"},
  "test_evidence_drilldown.py": {"expected_ok": 12, "note": "evidence drilldown; deterministic content-derived research evidence IDs (url+excerpt hash, whitespace-invariant, title-independent so cache rows and parsed rows agree), registry owned by maya_evidence with one writer (register_answer: idempotent for identical answers, capped to the most recent answers) and one reader (load_answers), honest nothing_registered for source-less answers, answer analysis delegated to the research owner (analyze_rows/_conflict_index_map in maya_web_research) so the registry verdict always matches the rendered summary, research drilldown exposes topic/supports/source/type/confidence/retrieved/freshness/conflicts/answer-context with contradiction linkage inside the answer, world drilldown exposes claim/source/type/timestamps/freshness/conflicts/uncertainty/visibility through the store's public read_evidence door (no external file access), unambiguous 8+ char prefix lookup on both stores with ambiguity protection, freshness clock-injectable (stale/fresh/unknown), help/miss paths honest, show_sources pure over injected answers, drilldown/show_sources write-free (registry and world store byte-identical), owner-side extract_summary_evidence dual capture path (cache replay rows with titles and parsed summary rows) yielding identical IDs, matching Status line, and UTC->Z normalization, maya_topic_research tags answers with Evidence IDs via the owner functions without altering the summary body, empty answers register nothing, router :evidence <id> loads answers and renders while bare :evidence still serves the cluster report JSON, show sources/evidence sources aliases wired, :world list carries the drilldown pointer, live-usage registry appends tolerated, real registry untouched by the suite"},
  "test_capability_awareness.py": {"expected_ok": 8, "note": "capability awareness; single-owner registry (maya_capabilities.CAPABILITY_REGISTRY) feeds every self-description surface, no phantom capabilities (every advertised bare colon command dispatches through maya_local_command; the removed :interests cannot return), templated and advertised NL forms reachable (continue-the-thread resolves to an honest refusal against empty state), controls_line/local-controls/report/summary derivations all consistent with the registry, contextual hints (capability_hint) compose from each registry entry and return empty for unregistered groups so removed capabilities stop being hinted, degraded-mode honesty (offline fallback greeting equals owner reply plus the registry-derived offline-safe line; offline_registry_audit catches stale safe-names, safe/unsafe overlap, and behavioral leaks; improvement questions degrade to the approval-path answer with the pinned governance line), honesty bounds (read-only policy, not expert review, approval-gated changes, no application control) present on conversational and report surfaces, NL capability phrases resolve to the identical registry-derived summary from router/local/ask entry points, :capabilities serves the grouped report, :help derives from colon_commands with no phantoms, drift guard proves adding a registry entry updates every surface at once"},
  "test_restoration_math_knowledge_orchestration.py": {"expected_ok": 53, "note": "8R restoration coverage: cooperative/zero-engagement math kept, world analyze dispatch/status/additive/NL-parity/advertised, knowledge-network search render/refusal/dispatch, graph determinism + review-only provenance + offline gate (no file when offline, live writes, file line count) + honest term rendering + dispatch, :knowledge/+:knowledge search/+:knowledge graph/[term] advertised + :knowledge status dispatch, offline registry audit, model default value + chat-uses-registry + model listed, capability alias names/registry match + total count + summary-derived, prompt coverage of every advertised surface, restoration_integration pass"},
  "test_restoration_runtime_device_bridge_loop.py": {"expected_ok": 57, "note": "8R runtime device bridge loop: device profile (os/machine/python/CPU/memory/device class/fail-closed/positive/summary/deterministic structure), offline sets disjoint + suitability covers all groups, capability bridge consistency + full display-group and math-slug coverage + every mapped slug registered + per-group lookup + coverage summary, unified self report (What Maya can do, runtime model, qwen2.5-coder:3b, local device, capability layer, bridge), command surfaces (:device/:device status/:show device profile/:what hardware/:capabilities detail/:detailed capability report), digest seed recorded/two/evidence count/status/all finite/runtime series, loop world-structure ledger bound + series fold + deterministic + derived series skips digest + loop never writes, loop-to-evidence bridge"},
  "test_trusted_capabilities.py": {"expected_ok": 28, "note": "trusted-capability registry: available ONLY when the full battery validates (identity/provenance, availability probe must affirm, capability scope, context, actions, approval, io boundaries, auth state, result confirmation, fail-closed failure, cross-scope isolation); no validated record = refused; not-installed external systems unavailable; installed providers never auto-trusted; revoked stays revoked through routine validation, only an explicit grant restores, invalid grants rejected; capability change/contradiction/malformed/unregistered-action/auth-expired/fail-open/unverified/timeout/exception/cross-context-leak all fail closed; scope denial refused while record stays available; cross-context and optional-owner cross-user access refused; prepare-only allowed only for the declared safe set and never execution; approval-required honored (owner approval), unapproved denied as requires_approval; action records written per decision with deterministic timestamp; registry overlay resolves every advertised inventory capability to a deny state unless a validated provider serves it; live registry never grants availability to merely-listed capabilities; suite never writes the real registry or action log"},
  "test_trust_commands.py": {"expected_ok": 30, "note": ":trust command surface: router dispatch parity (:trust handled, :trustless/:nonexistent-verb stay None), every subcommand returns a string, unknown subcommand returns honest usage, empty registry shows every advertised capability pending_validation and nothing trusted, listed-but-unvalidated capabilities stay pending, available/trusted shown only after the full battery validates, :trust list/:trust show mark trusted state, :trust pending excludes the validated serving provider, revocation tombstones listed with revoked state, validate/grant/revoke are confirmation-gated and write nothing without the confirm token, scope shows owner only when present and never fabricates organization, secret material and source digests never rendered (inline hex and key=value secrets redacted, identity exposes provider/version plus digest-recorded bool only), action records render decision/approval_required/owner_approval/verification_supported/result, absent action log reported honestly, :trust stays unadvertised (not in colon_commands/command_list/:help) so capability-awareness contracts are unchanged, :capabilities surface unchanged, real registry/action log untouched"},
  "test_intelligence_core.py": {"expected_ok": 31, "note": "Maya Intelligence Core presentation model: nine states (idle/observing/reasoning/approval/acting/verifying/complete/unavailable/error) resolve deterministically from authoritative activity+resource signal, the validated-capability overlay and the latest authorization/verification decision; complete (the only success state) requires affirmative authorization AND verification; missing/empty/malformed/unknown authority data and malformed/unavailable resources fail closed to unavailable, unsafe resources to error; unavailable/error carry no active capability/authority/task/trust node or indicator and never imply success; calm/active states never imply success; bounded deterministic unit-space geometry (contours, dashed orbital rings, signal lines, connection nodes) and a self-consistent digest; IntelligenceCore.qml is presentation-only and schema-gated (Home/Main no longer mount a FaceView; the canonical face pipeline stays Python-only) and Main routes core JSON; the pure bridge exposes core additively (live_lines length unchanged) and reset stays None; real-runtime drive through the actual trust gate + action log yields complete after validate+allow and unavailable after revoke; real registry/action log never written"},
   "test_ux_redesign.py": {"expected_ok": 18, "note": "student UX / visual redesign: IntelligenceCore.qml is now an abstract animated orb/pulse (breath/spin/pulse phases via declarative QML animation only - no wall clock, timers or randomness) with the student palette mint ready / electric thinking / soft gold insight / red attention and a fail-closed schema gate; the facial figure is removed from the runtime surfaces (no FaceView or setFace in Home/Main) while the canonical vector-face and shape Python pipelines are kept for their own suites; Home is chat-first with three Quick Action cards routed through ui.chatSend, a plain System Healthy indicator (healthy only on a non-failure core with safe resources) and the technical activity log + trust list moved behind an Advanced / Developer dropdown; a dismissible WelcomeOverlay explains the orb colours in student language and Main hosts it with Home help re-opening it; plain-language vocabulary replaces Presence Mode -> Maya's Status and Research Cache -> Saved Knowledge across Settings/Control and navigation; the additive student bridge block (status_label/status_tone/status_detail/state/healthy/core_legend) maps all nine authoritative core states to labels and tones, is deterministic, keeps live_lines at 7 and the existing keys intact, and can never show Ready for a failure state (error -> Something's wrong/red, unknown/missing -> Paused/muted)"},
   "test_trust_live_validation.py": {"expected_ok": 25, "note": "real-life :trust validation matrix: real inventory read via the router reports advertised capabilities pending and trusted only after validation; live validate confirms trust then authorize allows; revocation is immediate and routine re-validation cannot restore it; explicit grant (controlled path with probe evidence) restores the registry permission and authorize still gates every action; not-installed external systems show unavailable and are refused; cross-context, out-of-scope, and cross-user access refused with the record still available; approval-required blocks as requires_approval until owner approval; prepare-only yields prepared_not_committed and never an execution/authorization result; timeout, exception, unverified, contradictory, and malformed records all fail closed; every action record carries capability/user/context/decision/approval_required/owner_approval/verification_supported/result/timestamp; each case emits one JSON record of capability, system/identity, context, requested action, expected vs observed decision, execution result, verification result, action-record presence, and pass/fail; real registry and action log never written"},
  "test_frequency_architecture.py": {"expected_ok": 21, "note": "frequency architectural layer: pure stdlib substrate (radix-2 FFT with power-of-two padding, deterministic dominant frequency, bounded autocorrelation periodicity with a local-max gate so a drift scores 0, finite SNR separating tone from noise, complementary noise ratio, Parseval Haar energy, bounded spectral alignment, invariant audit) with corrupt input raising at the boundary; the frequency layer returns a structured deterministic report and a fail-closed quality gate (positive SNR AND non-trivial periodicity AND bounded noise; invalid/short refused); coordinator MathAgent and the PatternAlignment runtime facade expose the same vocabulary; the trust battery's new signal_integrity check is strictly additive (not-declared passes), admits coherent periodic evidence, fails closed on noisy/malformed evidence, honors per-record frequency thresholds, and the action vocabulary admits install_dependency while unregistered actions still fail closed"},
  "test_frequency_external_validation.py": {"expected_ok": 6, "note": "frequency external verification: static power-of-two dataset (constant/trend/periodic/mixed/noise cases incl. a sample-rate-scaled tone) in verification/frequency_dataset.json; clean-room naive O(N^2) DFT oracle (verification/oracle_frequency.py, stdlib-only, never imports maya_math/maya_frequency) agrees with the substrate FFT to 1e-9 on dominant frequency, periodicity, SNR, noise ratio and centroid for every case; substrate, oracle and the architectural report all classify each case to the pinned class; the quality gate matches each case's declared expectation; the corpus is deterministic"},
  "test_full_system_integration.py": {"expected_ok": 16, "note": "full-system architectural integration (long thread + provisioning recovery + fail-closed + latency + integrity): samples -> spectral substrate -> frequency layer -> MathAgent/PatternAlignment agree on class/dominant-frequency/periodicity/SNR margin, coherent periodic evidence validates available and authorize allows while broadband noise validates untrusted and is refused, and the real decision drives the visual state model to complete/unavailable with zero active indicator/node on failure; an installed dependency is removed (simulated outage) dropping the dependent capability to unavailable (all actions refused) then gated provisioning reinstalls it through the sandbox runner and the capability recovers to available with strictly monotonic available<outage<recovery<revoked<granted recovery timestamps; explicit revoke->grant round trip; the visual transition complete->unavailable->approval->complete carries no error/invalid state and repeated identical ticks give byte-identical digests (no flicker); malformed spectral evidence (None/empty/non-numeric/non-finite/boolean/text/bytes/object) is refused at analyze/quality/classify/invariants/MathAgent/PatternAlignment and poisons trust to untrusted; contradictory trust (absent provider, raised/timeout probe, revoked, malformed, unknown verdict, unsafe resources, malformed/empty overlay) all resolve to a failure state with zero active indicators/nodes (no ghost authority) while only available+non-failure indicators activate; documented latency budgets hold (frequency analyze/quality/FFT < 25 ms on a 128-sample window, authorize < 30 ms, full validate < FRAME_MS_DEFAULT, spectral validation overhead < 25 ms, core projection < 16 ms, UI tick < 3x frame budget) and a spy proves zero spectral analysis calls per UI tick; all nine visual states reachable and deterministic; real registry/action log byte-untouched"},
  "test_provisioning_recovery.py": {"expected_ok": 13, "note": "provisioning recovery: read-only plan and allowlist (unknown dependency refused before any gate); default deny when no validated provider record exists (refused, nothing executed); owner confirmation token mandatory and bound to capability+dependency (wrong/absent token runs nothing and writes nothing); execute defaults to False (prepared_not_executed) and execution without a runner is refused; the registered provisioning provider authorizes an install and an injected sandbox runner installs the missing module (available_before False -> available_after True), a second run reports already_available; a dependent provider stuck unavailable is refused and recovers to available only after the dependency is provisioned, after which authorize allows; the signal_integrity gate composes (clean available/allowed, noisy untrusted/refused); temporary registries only, and the real registry/action log are byte-untouched"},
  "test_orchestration_lifecycle.py": {"expected_ok": 26, "note": "request-lifecycle orchestration + conversational pipeline: one message becomes one request with a stable correlation id and optional idempotency key (duplicate submission returns the original request and never re-executes), an explicit received->planning->validating->executing->verifying->completed state machine ending in exactly one terminal outcome (completed/denied/cancelled/timeout/unavailable/error/refused), bounded idempotent retries (error/unavailable re-armed via resolve retry, never a second terminal outcome), distinct failure classes (backend_unavailable vs approval_required vs action_refused vs internal_error) with the diagnostic reason preserved, fail-closed handling of empty/non-text payloads and malformed plans/executor results, clarification awaiting the user, deadline timeout, outcome-rendered responses that never claim completion unless confirmed, the real maya_chat pipeline (deterministic route, stubbed model path, model-unavailable fallback) reaching the same path, and the student UI bridge exposing the actionable pending queue additively (live_lines stays 7) with the real trust artifacts untouched"},
  "test_pending_validation.py": {"expected_ok": 26, "note": "pending-validation lifecycle: every item carries id/kind/title/summary/required_action/next_step/status/timestamps/expiry/reason/attempts with a stable deterministic id and unknown statuses failing closed to unavailable, exactly one terminal outcome (approved/denied/expired/cancelled/failed/unavailable), reject/dismiss aliases, retry re-arms failed/unavailable items (bounded attempt) but is refused for open/other-terminal items, double resolution is idempotent and never duplicates, an explicit controls matrix, deterministic TTL sweep (expires only open items at/after expires_at, no-TTL never expires, terminal never swept), deterministic actionable surface ordering (open first, newest first) with specific item/required action/next step/controls, summary and human-summary text, an in-memory clock-injectable store (duplicate id rejected, unknown item reported, clock-driven TTL), the explicit chat `:pending` command surface (list/approve/deny/usage/unknown), inputs never mutated, and the real trust artifacts untouched"},
  "test_instrumentation.py": {"expected_ok": 25, "note": "read-only performance instrumentation core (opt-in, in-process memory only): disabled by default with a true no-op (record/span/trace_request/startup_report all inert), enable+record stores valid duration/size/count/meta; injectable monotonic clocks give exact durations and refuse backwards/None/non-numeric timings; a raising clock never breaks a request (invalid timing dropped, request still completes); bounded event/trace deques cap a flood and 300 repeated turns create no threads/observers (no leak); meta is key-allowlisted so message content can never be recorded (a secret passed as text/reason/summary is absent from to_json) and a real chat turn's user text is absent from the exported JSON; trace_request carries request_id/state/outcome/outcome_class/terminal/attempts/duplicate/planning_ms/validation_ms/execution_ms/approval_wait_ms/total_ms for the completion, approval (approval_wait measured), denial (execution blocked), cancellation (execution blocked), bounded retry, timeout and duplicate paths; a conflicting second terminal outcome is flagged; startup phase marks (bridge_ready/qml_engine_load/main_window/first_frame/first_input) and resource snapshots (cpu/rss/threads/handles) are exposed; spans never swallow upstream errors; disabled/enabled runs create no threads; the module opens no files and the real trust artifacts are byte-untouched; the Qt app declares the ui_tick/pending_panel_update/startup hooks guarded by is_enabled()"},
   "test_performance_contracts.py": {"expected_ok": 5, "note": "instrumentation observable contracts (black-box): running the chat CLI with MAYA_INSTRUMENT=1 produces byte-identical stdout to a plain run with exit 0 and adds no [perf]/instrument/[face] output; importing the module and enabling+recording emits nothing on stdout/stderr; a chat turn reaches exactly one terminal completed outcome and a non-empty response with instrumentation on or off; malformed (whitespace-only) input fails closed to error with a non-claiming response; and the real trust artifacts are byte-untouched after an instrumented run"},
   "test_async_orchestration.py": {"expected_ok": 26, "note": "asynchronous orchestration driver: a turn runs off the caller's thread with a stable correlation id/generation and exactly one terminal outcome; results for a conversation are delivered in acceptance order even when the work finishes out of order (including an approval continuation after an interleaved turn); sessions are independent and concurrent turns all complete; the worker pool is bounded (running <= workers, queued <= queue) and queue overflow yields an explicit internal_error async_queue_full instead of a silent drop; a per-session in-flight cap cancels the oldest queued turn; cancel-before-execution removes queued work without running it and cancel-during-execution stops cooperative work with a cancelled outcome and reconciled pending item; timeout/approval-required/denied/refused/backend-unavailable/worker-exception/malformed-payload keep their distinct honest outcomes; duplicate idempotency keys never execute twice; superseded late results are suppressed; a settled request never yields a second terminal outcome; shutdown and session close cancel queued work, suppress late delivery and leave no worker thread behind; the real trust artifacts are untouched"},
    "test_async_ui_contracts.py": {"expected_ok": 7, "note": "Qt UI-thread affinity contracts (offscreen harness on the PySide6 venv): the controller owns a bounded worker pool (finite workers and queue); _emit_view, _apply_pending_line and _set_activity mutate the bridge/ticker model on the Qt UI thread even when invoked from a worker thread; a slow worker does not block the UI event loop (timer ticks continue); the pool never exceeds its bounds under load and rejects overflow; closeNow joins every worker and stops accepting further work; the harness reports every expected check"},
    "test_chat_process.py": {"expected_ok": 19, "note": "bounded chat-subprocess lifecycle (deterministic fake child): normal completion delivers the reply with a stable correlation id/generation; [face]/[semantic]/[pending] lines are forwarded in order and only to the active turn while startup banner text goes to the idle channel; delayed work completes; cancel-before-launch removes queued work without running it; cancel-during-execution cooperatively terminates then forcibly reclaims the child; turn timeout terminates the child; crash/nonzero exit, malformed empty reply and partial output are distinguished honestly; launch failure is an explicit outcome; duplicate request ids never run twice and never emit a competing result; a late line after completion never creates a second terminal; page close drains queued+active turns and joins every thread; shutdown is idempotent; idle TTL reaps an unused child; the queue is bounded; stderr is forwarded; no orphan process or thread survives close"},
    "test_chat_process_ui.py": {"expected_ok": 7, "note": "Qt chat-subprocess integration (offscreen harness on the PySide6 venv, deterministic fake child via MAYA_CHAT_SCRIPT): the real controller owns a bounded chat manager and warms the child; a normal turn streams the reply and returns the UI to idle; chat state updates flow through the UI-thread dispatch path; a crashed child yields an honest non-completion line and marks chat offline; an open pending item is reconciled to a terminal state when the child dies; closeNow terminates the child and leaves no orphan process or manager thread; the harness reports every expected check"},
    "test_cancellation_stale.py": {"expected_ok": 23, "note": "Phase 3 cancellation & stale-result prevention (offscreen Qt harness on the PySide6 venv plus Tk/chat/executor arms on every interpreter): measurement guards (a clean projection trips no suppression/drop/cancel counter and generation/commit bookkeeping stays consistent); queued projection cancelled before start is removed and never delivered (and not counted as a late cancellation); a running projection cancelled cooperatively suppresses its late value (nothing delivered, nothing commits); a stale result landing after a newer frame committed is suppressed (_proj_superseded) and can never overwrite the committed view; a stale failure is equally suppressed - no reset emission, no drop count - while the newer generation still delivers; delivered ordering strictly follows commit order with the final view equal to the last submission; a concurrent burst keeps the committed generation monotonic and consistent; a failure from the current generation fails closed (reset view + counted drop) and recovers; closeNow stops accepting projections, joins the worker pool and nothing delivers afterwards; a projection stuck in time.sleep(30) never hangs closeNow or the executor join (shutdown returns an explicit leftover-worker count and join_timeout outcome, submit-after-shutdown stays rejected, pre/post thread audit shows no new threads) and the degraded-mode one-shot fallback is bounded (one daemon thread gated per N drops) with degraded_path_used routed summary-only into the bounded aggregate; the Tk compute worker drops stale tick_ready/tick_failed (_stale_skips) without regressing the applied state or adding fail-closed resets, keeps committed ordering monotonic and final==latest, and a non-dict reset advances the committed generation; ChatProcessManager: queued cancel reports queued with terminal cancelled, running cancel reports running with terminal cancelled, rejected duplicates/cancelled turns never produce a competing completed reply under a live id, and a child exiting without a reply yields a non-completion (partial) terminal outcome; BoundedExecutor: queued cancel never runs and on_done never fires, running cancel suppresses the late value with on_cancelled firing and state cancelled, and shutdown returns an explicit leftover_workers/join_timeout/elapsed dict (a cooperatively-uninterruptible block never hangs the join and reports join_timeout with a leftover worker, submit stays rejected, a clean drain reports 0/False on the idempotent second call); the harness reports every expected check"},
    "test_output_supervision.py": {"expected_ok": 83, "note": "post-generation output supervision boundary between model output and emission in maya_chat.ask: compliant answers pass through unchanged; identity/owner/capability/epistemic/authority/action/hold violations are repaired to canonical statements or rejected with reason codes and fallbacks; the model is never called as a validator (one urlopen per turn); decisions are deterministic; the boundary is pure (import allowlist, no filesystem/network surface); it fails closed when the authoritative source is unavailable, empty or partial; the Qt emission surface is untouched (chatLine reader + [face]/[semantic] channels intact, no supervisor wiring) and Tk remains the last-resort fallback; existing regression source invariants are intact; and the live ask() generation path actually runs the boundary (monkeypatched urlopen, no Ollama required)"},
    "test_bridge_updates.py": {"expected_ok": 23, "note": "bridge update change-detection + coalescing: the pure bridge exposes canonical equality, volatile-animation-field-excluded view fingerprints, changed-key diffs, sanitized versioned deltas, an input signature, a monotonic ChangeTracker and a bounded latest-state UpdateCoalescer that always bypasses for meaningful events (approval/denial/refusal/cancellation/timeout/failure/completion/safety/conversation) and force-flushes once; the offscreen Qt harness proves the real Controller suppresses identical and volatile-only views, coalesces rapid real changes to the latest state within the bounded window, keeps the pending update bounded, bypasses coalescing for meaningful events and safety transitions, stamps a strictly increasing revision, resets immediately on lifecycle reset, emits a versioned sanitized delta, and force-flushes a held update when the window closes (pending panel and chat surfaces unchanged, live_lines stays 7)"},
    "test_trust_cache.py": {"expected_ok": 36, "note": "trust-runtime metadata cache (contract + security/ethics + cross-layer): the cache stores only parsed registry metadata keyed by file identity/size/mtime plus a content hash, is invalidated by explicit store_record mutation (refresh/grant/revoke), malformed transition, TTL and reset, and never stores an authorization decision; miss/hit, valid/missing/malformed registries, malformed records, registry file change, content change with restored timestamp (strict verify) and size change, explicit grant/revoke tombstones, owner/context/scope/unknown-capability/approval-required/unsafe-core/empty-state handling, TTL expiry, clear, concurrent readers, cache corruption fallback, bounded LRU size and total-row budget, process restart, and no stale authorization; security checks prove cached metadata never becomes cached permission, revocation is immediate, approval is never inferred, a different user/context/action cannot reuse a decision, malformed/ambiguous data stays untrusted, unavailable never becomes silently trusted, the four honest outcomes stay distinct, and diagnostics leak no secrets; cross-layer checks confirm the trust runtime, action log, intelligence-core projection and :trust diagnostics never show allowed/approved/completed when the runtime refuses"},
    "test_retention_mailbox.py": {"expected_ok": 17, "note": "Phase 4 retention & mailbox bound: BoundedExecutor releases terminal callables (fn/args/kwargs dropped, released counted, live state/result still readable, evicted handle() returns None = existing unknown-id semantics), tracking stays capped at max_tracked once settled with oldest-completed-first eviction of terminal handles only, queued cancellation still removes work before run and terminal/unknown cancel resolves None, a running job observing its cancelled token suppresses the late value (cancelled, result None, cancel_reason late_result), delivered results keep ordering/identity invariants under a capped registry, and shutdown cancels queued work cooperatively, joins promptly leaving at most max_tracked terminal handles, rejecting new submissions; maya_app.Mailbox never exceeds the 512 cap under flood (capacity holds, drop counter tracks every drop), content (chat/log) keeps the newest message by retiring the oldest so a flood can never silence the newest line (incl. emergency), refresh state drops newest at the door when full, the rate-limited [_mailbox] UI behind truncation banner surfaces once output drops, the drained box ends empty with the poll-style get_nowait/queue.Empty loop intact, and the tick generation stream stays strictly non-decreasing with the _stale_skips/_last_committed_gen/poll guard wiring intact; suite is headless and writes no files"},
    "test_observability.py": {"expected_ok": 14, "note": "Phase 4 runtime observability (bounded, opt-in, in-process only): maya_instrumentation merges numeric flow counters into a bounded runtime aggregate (max 64 keys) via report_runtime_counts, which is summary-only and never appends an event, and exposes it through diagnostics()/snapshot()/to_json() with reset clearing it and a disabled module fully no-oping (no threads, no side effects); synthetic counter magnitudes merge exactly (non-numeric/malformed inputs ignored) and the key map stays bounded under a >64-key flood; the Tk app emits exactly one runtime_summary per poll interval (never per interaction, sub-interval bursts add none) carrying mailbox drops, mail drops, tick_skips, stale_skips, tick_ready, compute queue depth and compute-worker liveness with a clean run reporting zero drops/skips and real counter values flowing through unchanged; the offscreen Qt harness proves a clean projection+delivery trips no suppression/drop/supersede/cancel counter and the exporter sees those zeros, real suppression (identical resubmission), delivery and bounded-drop paths surface with proj_/suppressed/emitted/unchanged plus work_/project_ executor stats, and closeNow always flushes a final summary (aggregate phase closeNow, metadata advanced, projection and executor keys present in every runtime_summary event with source tk/qt only); suite is headless and writes no files"},
    "test_market_pattern_module.py": {"expected_ok": 16, "note": "trading-sector MPM engine (maya_runtime.market.mpm, math/typing/dataclasses only): schema vocabulary pinned against maya_identity/market/mpm.schema.json definitions; byte-deterministic repeated interpretation; stateless across interleaved windows; malformed/bad-enum/non-finite/short series raise at the boundary; uptrend fixture (10 closes 100->110) traces breakout 0.65 + uptrend signal 1.0 + medium volatility + 1x overextension(medium) + sentiment positive user_provided; range fixture (~99.7-100.5) traces range_consolidation 0.70 + sideways small-signal + low volatility + zero flags; full required/closed key-set conformance; every instrument type X timeframe accepted; EMA(9) math agrees with an independent reference; RSI(14) neutral 50.0 on short windows and in [0,100] on long windows; volatility ATR buckets low/medium/high exact; trend classification uptrend/downtrend/sideways; risk flags capped at 5 and clean on range; sentiment always source=user_provided with deterministic lexicon tones; explanation directive-free; native and MAYA_RUNTIME_HEADLESS=1 arms byte-identical"},
    "test_market_analyst_persona.py": {"expected_ok": 14, "note": "trading-sector market_analyst persona runtime (maya_runtime.persona.market_analyst): profile conforms to market_analyst_persona.schema.json; persona ceilings within CHANNEL_MAX and equal to PERSONA_CEILINGS runtime record; allowed registers fresh subset of even/technical/warm with banned registers rejected; closed vocabulary (interpretation terms, banned directive terms) matching the schema enums; MP01-MP10 hard rules present; persona directive bans subset of the safety blocklist; shaping stateless (byte-identical repeat); shape output closed key-set with stated_confidence capped at 0.5; register domain enforced (playful rejected); PersonaSeal tenant-scoped/cross-tenant-denied; escalation ladder E0 rephrase/E1 neutralize/E2 revoke_ready/E3 halt plus violation-count drive and trap-limit branch; shaped explanation neutral + disclaimer template appended with violations linting clean/dirty; native and MAYA_RUNTIME_HEADLESS=1 arms byte-identical"},
    "test_financial_safety_rules.py": {"expected_ok": 14, "note": "trading-sector financial-safety governance (maya_identity/market/financial_safety_rules.json via the persona governor): ruleset conforms to financial_safety_rules.schema.json; ruleset_version pinned 1.0 with interpretation_only posture and no_recommendation_no_prediction state; directive_terms blocklist exact (buy/sell/hold/long/short/should/must/guarantee/guaranteed/risk_free/certain/predict/target_price/profit/breakout_buy) and every term lints; four forbidden_phrase classes (profit_guarantee/certainty/recommendation/future_price) present and lintable; confidence_cap 0.5 pinned to the schema const; always_statements present; disclaimer_template exact and directive-free; escalation E0-E3 strings pinned with rephrase/neutralize/revoke READY/halt semantics; sandbox pins (external_apis/account_access/data_egress/order_routing) all false; output compliance frame (shape directive_free + capped stated_confidence + disclaimer suffix + phrase trap); native and MAYA_RUNTIME_HEADLESS=1 arms byte-identical"},
}

FAST_SUBSET = (
    "test_rig_math.py",
    "test_readiness.py",
)

CONTROLLED_RUNTIME = {
    "module": "maya_runtime",
    "headless_env": "MAYA_RUNTIME_HEADLESS",
    "signature_ops": (
        ("pattern_state", (0.0, 1.0, 0.5)),
        ("cosine_similarity", ((1.0, 0.0), (1.0, 0.0))),
        ("world_stability", None),
        ("channel_max", None),
    ),
}


def all_suites():
    return SUITES